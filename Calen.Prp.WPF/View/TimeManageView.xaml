<UserControl x:Class="Calen.Prp.WPF.View.TimeManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Calen.Prp.WPF.View"
             mc:Ignorable="d" 
             >
    <Grid >
       
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        <Border  Background="{DynamicResource PrimaryLightColorKey}" Width="200" BorderBrush="{DynamicResource PrimaryMedianColorKey}" BorderThickness="0,0,1,0">

            </Border>

        <Grid Grid.Row="1" Grid.Column="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="{DynamicResource PrimaryLightColorKey}">
                <TextBlock Text="目标列表" FontSize="14" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </Border>
            <Border Grid.Row="1" Background="White"></Border>
        </Grid>
        </Grid>

   
</UserControl>
